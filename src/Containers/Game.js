import React from 'react';

const Game = (props) => {
  return (
    <div className="Home"
      style={{
        height: '100vh',
        display: 'flex',
        flexDirection: 'column',
        flex: 1,
      }}>
      In game
    </div>
  );
}

export default Game;
